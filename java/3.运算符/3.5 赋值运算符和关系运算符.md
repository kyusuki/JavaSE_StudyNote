### 一、赋值运算符 =
  作用：将右侧的值直接赋给左边变量（左侧必须是变量，不能是常量或表达式）
  变量=表达式

### 二、符合赋值运算符 +=，-=，* =，/=，%=
  变量 运算符=表达式

### 三、关系运算符 == ，!=，>，<，>=，<=
  基本类型（如int，double）：== / != 比较值是否相等
  引用类型（如String，自定义类）：== / != 比较对象地址值是否相同
  boolean类型不能与其他类型比较