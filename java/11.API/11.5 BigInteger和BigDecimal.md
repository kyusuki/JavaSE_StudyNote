### 一、**BigInteger**
```
public BigInteger(int num,Random rnd)        获取随机大整数，范围（0到2的num次方-1）
public BigInteger(String val)                获取指定的大整数
public BigInteger(String val,int radix)      获取指定进制的大整数
public static BigInteger valueOf(long val)   静态方法获取BigInteger对象，内部有优化
```

### 二、BigInteger底层存储方式
  `int signum`：符号位，用于表示整数的正负
    `signum`=1：整数
    `signum`=-1：负数
    `signum`=0：零
  `int[] mag`：存储整数绝对值的二进制补码，数组中每个`int`元素表示32为二进制数，用于拼接完整的大整数



### 三、**BigDecimal**
对象获取
```
    BigDecimal b1=new BigDecimal("1234567689.987654321");
    Bigdecimal b2=Bigdecimal.valueOf(0.1);
```

### 四、BigDecimal底层存储方式
  `value`字节数组：每个字节对应字符的`ASCII`编码
