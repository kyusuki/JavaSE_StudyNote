### 一、数字进行运算要转换成同种数据类型
  ==隐式转换==（自动类型提升）：
    把一个取值范围小的数据，转成取值范围大的数据
    byte short char三种类型的数据在运算时，都会直接先提升为int，然后在进行运算![[Pasted image 20250727170202.png]]

  ==强制转换==
    把一个取值范围大的数值，复制给取值范围小的变量
    格式：目标数据类型 变量名=(目标数据类型)被强转的数据