### 一、特点
  1. `TreeMap`跟`TreeSet`底层原理一样，都是红黑树结构
  2. 由键决定特性：不重复、无索引、可排序
  3. 可排序：对键进行排序
    注意：默认按照键的从小到大进行排序，也可以自己规定键的排序规则
  4. 在`TreeMap`中没有额外特有方法，可以直接使用`Map`里面的方法

### 二、排序两种方式
  1. 实现`Comparable`接口，指定比较规则
  2. 创建集合时传递`Comparator`比较器对象，指定比较规则