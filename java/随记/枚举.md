首先，枚举是一种特殊的类，可用于使用固定值的时候
枚举类的格式
    `修饰符 enum 枚举类名{`
        `枚举值1,枚举值2,...; //大写`
        `其他成员...`
    `}`
枚举类的特点
  枚举类的第一行只能罗列一些名称，这些名称都是常量，并且每个常量记住的都是枚举类的一个对象
  枚举类的构造器都是私有的，因此枚举类对外不能创建对象
  枚举类是最终类（`final`修饰），不可以被继承
  枚举类中，从第二行开始才可以定义类的其他各种成员
  编译器还为枚举类新增了其他几个方法，并且枚举类都是继承类，都继承于`java.lang.Enum`类