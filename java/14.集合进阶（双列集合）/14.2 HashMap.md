### 一、特点
  1. `HashMap`是`Map`里面的一个实现类
  2. 在`HashMap`中没有额外特有方法，可以直接使用`Map`里面的方法
  3. 特点是由键决定的：无序、不重复、无索引
  4. `HashMap`跟`HashSet`底层原理是一样的，都是哈希表结构

### 二、注意
  1. 依赖`hashCode`方法和`equals`方法保证键的唯一
  2. 
    如果键存储的是自定义对象，需要重写`hashCode`和`equals`方法
    如果值存储自定义对象，不需要重写`hashCode`和`equals`方法